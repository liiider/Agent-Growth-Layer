# Handoff: CI Workflow

Date: 2026-05-03

## Scope

Added GitHub Actions CI for:

- Python dependency install
- Python lint
- Python tests
- Python compile check
- JavaScript SDK dependency install
- JavaScript SDK TypeScript build

## Implementation Notes

- Uses `actions/checkout@v6`.
- Uses `actions/setup-python@v6`.
- Uses `actions/setup-node@v6`.
- Uses Node 24.
- Disables setup-node package-manager auto cache with `package-manager-cache: false`.
- Uses `npm ci` in CI because `package-lock.json` is committed.

## Verification Performed

Local equivalents:

```powershell
python -m pytest
python -m ruff check .
python -m compileall server sdk examples
npm.cmd ci
npm.cmd run build
```

Results:

- Python tests: 20 passed.
- Python lint: passed.
- Python compile: passed.
- npm ci: passed, 0 vulnerabilities.
- npm build: passed.

## Problems Encountered

No workflow-specific code issue. Local PowerShell still requires `npm.cmd` rather than `npm` because `npm.ps1` is blocked by execution policy.

## Remaining Work

After push, verify GitHub-hosted CI result on the repository Actions tab.
